apiVersion: apps/v1
kind: Deployment
metadata:
name: {{ include "bg.name" . }}-order-{{ .Values.activeColor }}
labels:
app.kubernetes.io/name: order-service
color: {{ .Values.activeColor }}
spec:
replicas: {{ .Values.app.replicas }}
selector:
matchLabels:
app.kubernetes.io/name: order-service
color: {{ .Values.activeColor }}
template:
metadata:
labels:
app.kubernetes.io/name: order-service
color: {{ .Values.activeColor }}
spec:
containers:
- name: order
image: "{{ if eq .Values.activeColor "blue" }}
{{ .Values.app.imageOrderBlue }}{{ else }}{{ .Values.app.imageOrderGreen }}
{{ end }}"
ports:
- containerPort: {{ .Values.app.containerPort }}
readinessProbe:
httpGet:
path: /health
port: {{ .Values.app.containerPort }}
initialDelaySeconds: 10
periodSeconds: 10
resources:
{{ toYaml .Values.app.resources | indent 8 }}
